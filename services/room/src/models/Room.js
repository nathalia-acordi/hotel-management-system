// Room.js — Model simples para uso com RoomFactory
export class Room {
  constructor(number, type, price, capacity) {
    this.number = number;
    this.type = type;
    this.price = price;
    this.capacity = capacity;
  }
}
